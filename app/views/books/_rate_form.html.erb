

  <%= form_for([@book ,@rate]) do |f| %>
    <div class="">
      <%= f.number_field 'rate', value: @rate.rate,  in: 1..5 , class: "span1"%>
      <%= f.submit "Rate", class: "btn btn-small btn-primary span1" %>
    </div>
  <% end %>
